# Contracts

Shared shapes that parallel streams build against (PLAN.md §3).

**Not frozen yet.** Phase 0 freezes them only after the Dafny spike works
end to end. Until then, `examples/spike-program.json` is the spike's subset
of the core program JSON in SPEC §5.1: `run`, `do` and `stop`, with literal
commands.
