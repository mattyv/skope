{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/mattyv/skope/contracts/event.schema.json",
  "title": "Event",
  "description": "One line of skope's JSON Lines output on stdout (SPEC §10). Exactly one event shape matches, chosen by `event`. Every shape has the common fields ts, run_id, skill, skill_hash, host, and optionally section and line; run_id, skill and skill_hash are null before a run exists. Nothing else is allowed.",
  "oneOf": [
    {
      "$ref": "#/$defs/run_start"
    },
    {
      "$ref": "#/$defs/run"
    },
    {
      "$ref": "#/$defs/check_cmd"
    },
    {
      "$ref": "#/$defs/check"
    },
    {
      "$ref": "#/$defs/ask"
    },
    {
      "$ref": "#/$defs/effect_start"
    },
    {
      "$ref": "#/$defs/effect_end"
    },
    {
      "$ref": "#/$defs/would_do"
    },
    {
      "$ref": "#/$defs/page"
    },
    {
      "$ref": "#/$defs/would_page"
    },
    {
      "$ref": "#/$defs/handoff_page"
    },
    {
      "$ref": "#/$defs/transfer"
    },
    {
      "$ref": "#/$defs/outcome"
    },
    {
      "$ref": "#/$defs/handoff_record"
    },
    {
      "$ref": "#/$defs/error"
    },
    {
      "$ref": "#/$defs/warning"
    },
    {
      "$ref": "#/$defs/locked"
    },
    {
      "$ref": "#/$defs/stale_lock"
    }
  ],
  "$defs": {
    "run_start": {
      "title": "RunStartEvent",
      "type": "object",
      "description": "The run began.",
      "properties": {
        "ts": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(\\.[0-9]+)?Z$",
          "description": "UTC, ISO 8601."
        },
        "run_id": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill_hash": {
          "type": ["string", "null"],
          "pattern": "^sha256:[0-9a-f]{64}$",
          "description": "null before a run exists."
        },
        "host": {
          "type": "string"
        },
        "section": {
          "type": "string",
          "description": "The section's display name."
        },
        "line": {
          "type": "integer",
          "minimum": 1
        },
        "event": {
          "const": "run_start"
        },
        "params": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "dry_run": {
          "type": "boolean"
        },
        "caller": {
          "enum": ["person", "agent"]
        },
        "run_dir": {
          "type": "string"
        },
        "skope_version": {
          "type": "string",
          "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+(-[0-9A-Za-z.-]+)?$"
        },
        "skope_build": {
          "type": "string",
          "pattern": "^[0-9a-f]{64}$",
          "description": "Build identity, bare hex (SPEC §7.2)."
        },
        "effects_hash": {
          "type": "string",
          "pattern": "^sha256:[0-9a-f]{64}$",
          "description": "The skill's scope, as --effects and approvals hash it (SPEC §7.4), so a run can be matched to what was approved."
        }
      },
      "required": [
        "ts",
        "run_id",
        "skill",
        "skill_hash",
        "host",
        "event",
        "params",
        "dry_run",
        "caller",
        "run_dir",
        "effects_hash",
        "skope_version",
        "skope_build"
      ],
      "additionalProperties": false
    },
    "run": {
      "title": "RunEvent",
      "type": "object",
      "description": "A run command finished.",
      "properties": {
        "ts": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(\\.[0-9]+)?Z$",
          "description": "UTC, ISO 8601."
        },
        "run_id": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill_hash": {
          "type": ["string", "null"],
          "pattern": "^sha256:[0-9a-f]{64}$",
          "description": "null before a run exists."
        },
        "host": {
          "type": "string"
        },
        "section": {
          "type": "string",
          "description": "The section's display name."
        },
        "line": {
          "type": "integer",
          "minimum": 1
        },
        "event": {
          "const": "run"
        },
        "cmd": {
          "type": "string"
        },
        "exit": {
          "type": ["integer", "null"],
          "description": "null when the command timed out or was killed."
        },
        "ms": {
          "type": "integer",
          "minimum": 0
        },
        "timed_out": {
          "type": "boolean"
        },
        "truncated": {
          "type": "boolean"
        },
        "stdout_hash": {
          "type": "string",
          "pattern": "^sha256:[0-9a-f]{64}$"
        },
        "stdout_tail": {
          "type": "string",
          "maxLength": 2048,
          "description": "Redacted, at most 2KB."
        },
        "after_would_do": {
          "type": "boolean"
        }
      },
      "required": [
        "ts",
        "run_id",
        "skill",
        "skill_hash",
        "host",
        "event",
        "cmd",
        "exit",
        "ms",
        "timed_out",
        "truncated",
        "stdout_hash",
        "stdout_tail",
        "after_would_do"
      ],
      "additionalProperties": false
    },
    "check_cmd": {
      "title": "CheckCmdEvent",
      "type": "object",
      "description": "A command check finished.",
      "properties": {
        "ts": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(\\.[0-9]+)?Z$",
          "description": "UTC, ISO 8601."
        },
        "run_id": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill_hash": {
          "type": ["string", "null"],
          "pattern": "^sha256:[0-9a-f]{64}$",
          "description": "null before a run exists."
        },
        "host": {
          "type": "string"
        },
        "section": {
          "type": "string",
          "description": "The section's display name."
        },
        "line": {
          "type": "integer",
          "minimum": 1
        },
        "event": {
          "const": "check_cmd"
        },
        "cmd": {
          "type": "string"
        },
        "exit": {
          "type": ["integer", "null"],
          "description": "null when the command timed out or was killed."
        },
        "ms": {
          "type": "integer",
          "minimum": 0
        },
        "timed_out": {
          "type": "boolean"
        },
        "truncated": {
          "type": "boolean"
        },
        "stdout_hash": {
          "type": "string",
          "pattern": "^sha256:[0-9a-f]{64}$"
        },
        "stdout_tail": {
          "type": "string",
          "maxLength": 2048,
          "description": "Redacted, at most 2KB."
        },
        "after_would_do": {
          "type": "boolean"
        }
      },
      "required": [
        "ts",
        "run_id",
        "skill",
        "skill_hash",
        "host",
        "event",
        "cmd",
        "exit",
        "ms",
        "timed_out",
        "truncated",
        "stdout_hash",
        "stdout_tail",
        "after_would_do"
      ],
      "additionalProperties": false
    },
    "check": {
      "title": "CheckEvent",
      "type": "object",
      "description": "A comparison was evaluated.",
      "properties": {
        "ts": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(\\.[0-9]+)?Z$",
          "description": "UTC, ISO 8601."
        },
        "run_id": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill_hash": {
          "type": ["string", "null"],
          "pattern": "^sha256:[0-9a-f]{64}$",
          "description": "null before a run exists."
        },
        "host": {
          "type": "string"
        },
        "section": {
          "type": "string",
          "description": "The section's display name."
        },
        "line": {
          "type": "integer",
          "minimum": 1
        },
        "event": {
          "const": "check"
        },
        "expr": {
          "type": "string"
        },
        "left": {
          "type": ["string", "null"]
        },
        "right": {
          "type": ["string", "null"]
        },
        "result": {
          "type": ["boolean", "null"]
        },
        "after_would_do": {
          "type": "boolean"
        }
      },
      "required": ["ts", "run_id", "skill", "skill_hash", "host", "event", "expr", "left", "right", "result", "after_would_do"],
      "additionalProperties": false
    },
    "ask": {
      "title": "AskEvent",
      "type": "object",
      "description": "A question was asked. probs, chosen and confidence are null when the backend failed; detail then says why.",
      "properties": {
        "ts": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(\\.[0-9]+)?Z$",
          "description": "UTC, ISO 8601."
        },
        "run_id": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill_hash": {
          "type": ["string", "null"],
          "pattern": "^sha256:[0-9a-f]{64}$",
          "description": "null before a run exists."
        },
        "host": {
          "type": "string"
        },
        "section": {
          "type": "string",
          "description": "The section's display name."
        },
        "line": {
          "type": "integer",
          "minimum": 1
        },
        "event": {
          "const": "ask"
        },
        "question": {
          "type": "string"
        },
        "kind": {
          "enum": ["choice", "yesno", "score"]
        },
        "probs": {
          "type": ["object", "null"],
          "additionalProperties": {
            "type": "number",
            "minimum": 0,
            "maximum": 1
          }
        },
        "chosen": {
          "type": ["string", "integer", "null"]
        },
        "confidence": {
          "type": ["number", "null"],
          "minimum": 0,
          "maximum": 1
        },
        "sure": {
          "type": "integer",
          "minimum": 0,
          "maximum": 100
        },
        "passed": {
          "type": "boolean"
        },
        "backend": {
          "type": "string"
        },
        "model": {
          "type": "string"
        },
        "ms": {
          "type": "integer",
          "minimum": 0
        },
        "request_path": {
          "type": "string"
        },
        "request_sha256": {
          "type": "string",
          "pattern": "^sha256:[0-9a-f]{64}$"
        },
        "after_would_do": {
          "type": "boolean"
        },
        "range": {
          "type": "array",
          "items": {
            "type": "integer"
          },
          "minItems": 2,
          "maxItems": 2,
          "description": "Score asks only."
        },
        "detail": {
          "enum": ["unavailable", "request_too_large"],
          "description": "Failed asks only."
        }
      },
      "required": [
        "ts",
        "run_id",
        "skill",
        "skill_hash",
        "host",
        "event",
        "question",
        "kind",
        "probs",
        "chosen",
        "confidence",
        "sure",
        "passed",
        "backend",
        "model",
        "ms",
        "request_path",
        "request_sha256",
        "after_would_do"
      ],
      "additionalProperties": false
    },
    "effect_start": {
      "title": "EffectStartEvent",
      "type": "object",
      "description": "A do command is about to run.",
      "properties": {
        "ts": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(\\.[0-9]+)?Z$",
          "description": "UTC, ISO 8601."
        },
        "run_id": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill_hash": {
          "type": ["string", "null"],
          "pattern": "^sha256:[0-9a-f]{64}$",
          "description": "null before a run exists."
        },
        "host": {
          "type": "string"
        },
        "section": {
          "type": "string",
          "description": "The section's display name."
        },
        "line": {
          "type": "integer",
          "minimum": 1
        },
        "event": {
          "const": "effect_start"
        },
        "cmd": {
          "type": "string"
        }
      },
      "required": ["ts", "run_id", "skill", "skill_hash", "host", "event", "cmd"],
      "additionalProperties": false
    },
    "effect_end": {
      "title": "EffectEndEvent",
      "type": "object",
      "description": "A do command finished.",
      "properties": {
        "ts": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(\\.[0-9]+)?Z$",
          "description": "UTC, ISO 8601."
        },
        "run_id": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill_hash": {
          "type": ["string", "null"],
          "pattern": "^sha256:[0-9a-f]{64}$",
          "description": "null before a run exists."
        },
        "host": {
          "type": "string"
        },
        "section": {
          "type": "string",
          "description": "The section's display name."
        },
        "line": {
          "type": "integer",
          "minimum": 1
        },
        "event": {
          "const": "effect_end"
        },
        "cmd": {
          "type": "string"
        },
        "exit": {
          "type": ["integer", "null"],
          "description": "null when the command timed out or was killed."
        },
        "ms": {
          "type": "integer",
          "minimum": 0
        },
        "timed_out": {
          "type": "boolean"
        }
      },
      "required": ["ts", "run_id", "skill", "skill_hash", "host", "event", "cmd", "exit", "ms", "timed_out"],
      "additionalProperties": false
    },
    "would_do": {
      "title": "WouldDoEvent",
      "type": "object",
      "description": "Dry run: a do command that didn't run.",
      "properties": {
        "ts": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(\\.[0-9]+)?Z$",
          "description": "UTC, ISO 8601."
        },
        "run_id": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill_hash": {
          "type": ["string", "null"],
          "pattern": "^sha256:[0-9a-f]{64}$",
          "description": "null before a run exists."
        },
        "host": {
          "type": "string"
        },
        "section": {
          "type": "string",
          "description": "The section's display name."
        },
        "line": {
          "type": "integer",
          "minimum": 1
        },
        "event": {
          "const": "would_do"
        },
        "cmd": {
          "type": "string"
        }
      },
      "required": ["ts", "run_id", "skill", "skill_hash", "host", "event", "cmd"],
      "additionalProperties": false
    },
    "page": {
      "title": "PageEvent",
      "type": "object",
      "description": "A page instruction paged a human.",
      "properties": {
        "ts": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(\\.[0-9]+)?Z$",
          "description": "UTC, ISO 8601."
        },
        "run_id": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill_hash": {
          "type": ["string", "null"],
          "pattern": "^sha256:[0-9a-f]{64}$",
          "description": "null before a run exists."
        },
        "host": {
          "type": "string"
        },
        "section": {
          "type": "string",
          "description": "The section's display name."
        },
        "line": {
          "type": "integer",
          "minimum": 1
        },
        "event": {
          "const": "page"
        },
        "text": {
          "type": "string"
        },
        "ok": {
          "type": "boolean",
          "description": "Did the pager command succeed."
        }
      },
      "required": ["ts", "run_id", "skill", "skill_hash", "host", "event", "text", "ok"],
      "additionalProperties": false
    },
    "would_page": {
      "title": "WouldPageEvent",
      "type": "object",
      "description": "Dry run: a page that wasn't sent.",
      "properties": {
        "ts": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(\\.[0-9]+)?Z$",
          "description": "UTC, ISO 8601."
        },
        "run_id": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill_hash": {
          "type": ["string", "null"],
          "pattern": "^sha256:[0-9a-f]{64}$",
          "description": "null before a run exists."
        },
        "host": {
          "type": "string"
        },
        "section": {
          "type": "string",
          "description": "The section's display name."
        },
        "line": {
          "type": "integer",
          "minimum": 1
        },
        "event": {
          "const": "would_page"
        },
        "text": {
          "type": "string"
        }
      },
      "required": ["ts", "run_id", "skill", "skill_hash", "host", "event", "text"],
      "additionalProperties": false
    },
    "handoff_page": {
      "title": "HandoffPageEvent",
      "type": "object",
      "description": "The page sent for a handoff (SPEC §8).",
      "properties": {
        "ts": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(\\.[0-9]+)?Z$",
          "description": "UTC, ISO 8601."
        },
        "run_id": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill_hash": {
          "type": ["string", "null"],
          "pattern": "^sha256:[0-9a-f]{64}$",
          "description": "null before a run exists."
        },
        "host": {
          "type": "string"
        },
        "section": {
          "type": "string",
          "description": "The section's display name."
        },
        "line": {
          "type": "integer",
          "minimum": 1
        },
        "event": {
          "const": "handoff_page"
        },
        "text": {
          "type": "string"
        },
        "ok": {
          "type": "boolean"
        }
      },
      "required": ["ts", "run_id", "skill", "skill_hash", "host", "event", "text", "ok"],
      "additionalProperties": false
    },
    "transfer": {
      "title": "TransferEvent",
      "type": "object",
      "description": "The run moved to another section.",
      "properties": {
        "ts": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(\\.[0-9]+)?Z$",
          "description": "UTC, ISO 8601."
        },
        "run_id": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill_hash": {
          "type": ["string", "null"],
          "pattern": "^sha256:[0-9a-f]{64}$",
          "description": "null before a run exists."
        },
        "host": {
          "type": "string"
        },
        "section": {
          "type": "string",
          "description": "The section's display name."
        },
        "line": {
          "type": "integer",
          "minimum": 1
        },
        "event": {
          "const": "transfer"
        },
        "from": {
          "type": "string"
        },
        "to": {
          "type": "string"
        }
      },
      "required": ["ts", "run_id", "skill", "skill_hash", "host", "event", "from", "to"],
      "additionalProperties": false
    },
    "outcome": {
      "title": "OutcomeEvent",
      "type": "object",
      "description": "The run ended. Always the last event.",
      "properties": {
        "ts": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(\\.[0-9]+)?Z$",
          "description": "UTC, ISO 8601."
        },
        "run_id": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill_hash": {
          "type": ["string", "null"],
          "pattern": "^sha256:[0-9a-f]{64}$",
          "description": "null before a run exists."
        },
        "host": {
          "type": "string"
        },
        "section": {
          "type": "string",
          "description": "The section's display name."
        },
        "line": {
          "type": "integer",
          "minimum": 1
        },
        "event": {
          "const": "outcome"
        },
        "outcome": {
          "enum": ["stopped", "paged", "handoff", "locked", "stale_lock", "invalid", "error"]
        },
        "reason": {
          "enum": ["explicit", "gate_failed", "command_failed", "ask_unavailable", "deadline", null],
          "description": "A handoff reason (SPEC §8.1), or null."
        },
        "ask_calls": {
          "type": "integer",
          "minimum": 0
        },
        "effects": {
          "type": "integer",
          "minimum": 0
        },
        "dry_run": {
          "type": ["boolean", "null"],
          "description": "null if the mode was never set."
        }
      },
      "required": ["ts", "run_id", "skill", "skill_hash", "host", "event", "outcome", "reason", "ask_calls", "effects", "dry_run"],
      "additionalProperties": false
    },
    "handoff_record": {
      "title": "HandoffRecordEvent",
      "type": "object",
      "description": "The handoff record was written.",
      "properties": {
        "ts": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(\\.[0-9]+)?Z$",
          "description": "UTC, ISO 8601."
        },
        "run_id": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill_hash": {
          "type": ["string", "null"],
          "pattern": "^sha256:[0-9a-f]{64}$",
          "description": "null before a run exists."
        },
        "host": {
          "type": "string"
        },
        "section": {
          "type": "string",
          "description": "The section's display name."
        },
        "line": {
          "type": "integer",
          "minimum": 1
        },
        "event": {
          "const": "handoff_record"
        },
        "path": {
          "type": "string"
        },
        "record": {
          "type": "object"
        }
      },
      "required": ["ts", "run_id", "skill", "skill_hash", "host", "event", "path", "record"],
      "additionalProperties": false
    },
    "error": {
      "title": "ErrorEvent",
      "type": "object",
      "description": "An error (SPEC §7.1).",
      "properties": {
        "ts": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(\\.[0-9]+)?Z$",
          "description": "UTC, ISO 8601."
        },
        "run_id": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill_hash": {
          "type": ["string", "null"],
          "pattern": "^sha256:[0-9a-f]{64}$",
          "description": "null before a run exists."
        },
        "host": {
          "type": "string"
        },
        "section": {
          "type": "string",
          "description": "The section's display name."
        },
        "line": {
          "type": "integer",
          "minimum": 1
        },
        "event": {
          "const": "error"
        },
        "code": {
          "type": "string",
          "pattern": "^E-[A-Z0-9]+(-[A-Z0-9]+)*$"
        },
        "stage": {
          "enum": ["parse", "lint", "args", "runtime"]
        },
        "file": {
          "type": "string"
        },
        "message": {
          "type": "string"
        }
      },
      "required": ["ts", "run_id", "skill", "skill_hash", "host", "event", "code", "stage", "message"],
      "additionalProperties": false
    },
    "warning": {
      "title": "WarningEvent",
      "type": "object",
      "description": "A warning (SPEC §7.1). stage is the stage that found it.",
      "properties": {
        "ts": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(\\.[0-9]+)?Z$",
          "description": "UTC, ISO 8601."
        },
        "run_id": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill_hash": {
          "type": ["string", "null"],
          "pattern": "^sha256:[0-9a-f]{64}$",
          "description": "null before a run exists."
        },
        "host": {
          "type": "string"
        },
        "section": {
          "type": "string",
          "description": "The section's display name."
        },
        "line": {
          "type": "integer",
          "minimum": 1
        },
        "event": {
          "const": "warning"
        },
        "code": {
          "type": "string",
          "pattern": "^W-[A-Z0-9]+(-[A-Z0-9]+)*$"
        },
        "stage": {
          "enum": ["parse", "lint", "args", "runtime"]
        },
        "file": {
          "type": "string"
        },
        "message": {
          "type": "string"
        }
      },
      "required": ["ts", "run_id", "skill", "skill_hash", "host", "event", "code", "stage", "message"],
      "additionalProperties": false
    },
    "locked": {
      "title": "LockedEvent",
      "type": "object",
      "description": "Another run holds the lock.",
      "properties": {
        "ts": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(\\.[0-9]+)?Z$",
          "description": "UTC, ISO 8601."
        },
        "run_id": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill_hash": {
          "type": ["string", "null"],
          "pattern": "^sha256:[0-9a-f]{64}$",
          "description": "null before a run exists."
        },
        "host": {
          "type": "string"
        },
        "section": {
          "type": "string",
          "description": "The section's display name."
        },
        "line": {
          "type": "integer",
          "minimum": 1
        },
        "event": {
          "const": "locked"
        },
        "holder_pid": {
          "type": "integer",
          "minimum": 1
        }
      },
      "required": ["ts", "run_id", "skill", "skill_hash", "host", "event", "holder_pid"],
      "additionalProperties": false
    },
    "stale_lock": {
      "title": "StaleLockEvent",
      "type": "object",
      "description": "An earlier run died holding the lock.",
      "properties": {
        "ts": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(\\.[0-9]+)?Z$",
          "description": "UTC, ISO 8601."
        },
        "run_id": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill": {
          "type": ["string", "null"],
          "description": "null before a run exists."
        },
        "skill_hash": {
          "type": ["string", "null"],
          "pattern": "^sha256:[0-9a-f]{64}$",
          "description": "null before a run exists."
        },
        "host": {
          "type": "string"
        },
        "section": {
          "type": "string",
          "description": "The section's display name."
        },
        "line": {
          "type": "integer",
          "minimum": 1
        },
        "event": {
          "const": "stale_lock"
        },
        "path": {
          "type": "string"
        },
        "holder_pid": {
          "type": ["integer", "null"],
          "minimum": 1,
          "description": "null when the lock can't be read or parsed (SPEC §7)."
        }
      },
      "required": ["ts", "run_id", "skill", "skill_hash", "host", "event", "path", "holder_pid"],
      "additionalProperties": false
    }
  }
}
